<% content_for :title, "Leaderboards — AI Finder" %>

<section class="page-head">
  <p class="hero-eyebrow">Leaderboards</p>
  <h1 class="page-title">Best AI tools and models, by task</h1>
  <p class="page-sub">Browse our judged categories, plus efficiency rankings for the models that answer fastest with the fewest tokens.</p>
</section>

<div class="lb-grid">
  <% @categories.each do |cat| %>
    <%= link_to leaderboard_path(cat[:key]), class: "lb-card" do %>
      <div class="lb-card-top">
        <div class="lb-card-icon" style="--icon-tint: <%= tool_monogram_color(cat[:name]) %>">
          <%= icon(cat[:icon].presence || "sparkles", size: 22) %>
        </div>
        <span class="lb-card-name"><%= cat[:name] %></span>
      </div>

      <p class="lb-card-desc"><%= cat[:description] %></p>

      <div class="lb-card-foot">
        <% if cat[:leader] %>
          <span class="lb-rank-badge">#1</span>
          <% if cat[:kind] == :efficiency %>
            <span class="lb-card-leader-name"><%= cat[:leader].tool.name %> · <%= cat[:leader].model_variant.name %></span>
            <span class="lb-card-score lb-card-metric">
              <%= format_efficiency_latency(cat[:leader].latency_seconds) %> / <%= format_efficiency_tokens(cat[:leader].total_tokens, unit: false) %>
            </span>
          <% else %>
            <span class="lb-card-leader-name"><%= cat[:leader][0].name %></span>
            <%= colored_score(cat[:leader][1], css_class: "lb-card-score") %>
          <% end %>
        <% else %>
          <span class="lb-card-empty">Scores rolling out</span>
        <% end %>
      </div>
    <% end %>
  <% end %>
</div>
