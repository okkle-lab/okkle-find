<% content_for :title, "Edit post — Admin" %>
<section class="page-head">
  <h1 class="page-title">Edit: <%= @post.title %></h1>
  <%= link_to "← all posts", admin_posts_path %>
</section>
<%= render "form", post: @post %>
